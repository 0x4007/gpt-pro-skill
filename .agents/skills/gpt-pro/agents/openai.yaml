interface:
  display_name: "GPT Pro Research"
  short_description: "GPT Pro requests and thorough research with durable jobs"
  default_prompt: "Use $gpt-pro to research this question thoroughly, cite primary sources, and retrieve the completed answer."
policy:
  allow_implicit_invocation: true
